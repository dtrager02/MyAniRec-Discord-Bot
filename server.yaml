# This file was generated using the `serve build` command on Ray v2.2.0.

import_path: modelserver:graph

runtime_env: {config: {num_cpus: 16}}

host: 0.0.0.0

port: 8000

deployments:

- name: Preprocessor

- name: Model

- name: DAGDriver
  route_prefix: /
